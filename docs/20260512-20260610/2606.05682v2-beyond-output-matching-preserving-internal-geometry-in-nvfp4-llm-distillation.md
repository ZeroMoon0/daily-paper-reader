---
title: "Beyond Output Matching: Preserving Internal Geometry in NVFP4 LLM Distillation"
title_zh: 超越输出匹配：在NVFP4 LLM蒸馏中保留内部几何结构
authors: "Fangbo Tu, Junhua Zhao, Chi Liu, Xin Chen, Haifeng Wu, Jian Wan, Srinivasan Manoharan"
date: 2026-06-04
pdf: "https://arxiv.org/pdf/2606.05682v2"
tags: ["query:compression"]
score: 9.0
evidence: 面向NVFP4大语言模型压缩的量化感知蒸馏
tldr: 大语言模型低精度推理需求增长，NVFP4量化感知蒸馏通过KL散度匹配输出恢复精度。但研究发现仅输出匹配会掩盖内部表示退化，导致推理和编码任务瓶颈。本文提出CKA-QAD方法，在蒸馏过程中通过CKA对齐层间Gram矩阵来保持内部表示几何结构，从而提升表示对齐与下游性能。实验表明该方法在Nemotron 3 Nano等模型上显著改善推理和编码精度，且训练开销小。贡献在于将CKA引导的表示对齐作为输出匹配的实用补充。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.05682v2/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1621, \"height\": 476, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.05682v2/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1696, \"height\": 293, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.05682v2/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1694, \"height\": 287, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.05682v2/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1698, \"height\": 302, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.05682v2/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1717, \"height\": 218, \"label\": \"Table\"}]"
motivation: 仅输出匹配的NVFP4量化感知蒸馏会掩盖内部表示退化，导致推理和编码任务性能瓶颈。
method: 提出CKA-QAD方法，通过CKA对齐层间Gram矩阵作为轻量正则项，在蒸馏中保留内部表示几何结构。
result: 在Nemotron 3 Nano和Qwen3-4B等模型上，CKA-QAD显著改善表示对齐，提升推理和编码任务精度，训练开销小。
conclusion: CKA引导的表示对齐是低比特LLM恢复中输出匹配的有效补充，适用于NVFP4量化。
---

## 摘要
随着大语言模型越来越多地部署在延迟和成本受限的生产环境中，对低精度推理（包括基于NVFP4的方法）的需求日益增长。量化感知蒸馏（QAD）通过训练量化学生模型匹配冻结的高精度教师模型的输出分布（基于KL散度损失），有助于恢复低比特量化下丢失的精度。在这项工作中，我们首先提供了QAD的表示层诊断：仅输出匹配可能掩盖内部退化，因为许多中间激活几何结构可以产生相似的教师对齐logits。通过CKA，我们发现仅使用KL散度的QAD相对于BF16教师会降低逐层表示相似性，尤其是在RL后训练模型中更为严重。这种漂移与推理和编码任务的下游瓶颈相关，表明低比特恢复需要保留内部几何结构，而不仅仅是匹配输出。基于这一发现，我们提出了\textbf{CKA-QAD}，一种用于NVFP4 QAD和低比特LLM精度恢复的CKA引导的表示对齐方法。该方法添加了一个轻量级正则化器，通过在蒸馏过程中通过CKA对齐逐层Gram矩阵来保留内部表示几何结构。在Nemotron 3 Nano和Qwen3-4B-Thinking-2507上，CKA-QAD显著改善了表示对齐，并以适度的训练开销提高了下游推理和编码精度。我们的研究结果表明，CKA引导的表示对齐可作为量化LLM恢复中输出匹配的实用补充。

## Abstract
Demand for low-precision inference, including NVFP4-based approaches, has grown as large language models are increasingly deployed in latency and cost constrained production environments. Quantization-aware distillation (QAD) helps recover accuracy lost under low bit quantization by training a quantized student to match the output distribution of a frozen higher precision teacher via a KL-divergence loss. In this work, we first provide a representation level diagnosis of QAD: output matching alone can mask internal degradation, because many intermediate activation geometries can yield similar teacher-aligned logits. Using CKA, we show that KL-only QAD can reduce layerwise representational similarity relative to the BF16 teacher, with especially severe drift in RL-post-trained models. This drift correlates with downstream bottlenecks on reasoning and coding tasks, suggesting that low bit recovery requires preserving internal geometry rather than matching outputs alone. Motivated by this finding, we propose \textbf{CKA-QAD}, a CKA-guided representational alignment method for NVFP4 QAD and low bit LLM accuracy recovery. The method adds a lightweight regularizer that preserves internal representational geometry during distillation by aligning layerwise Gram matrices through CKA. Across Nemotron 3 Nano and Qwen3-4B-Thinking-2507, CKA-QAD substantially improves representational alignment and improves downstream reasoning and coding accuracy with modest training overhead. Our findings position CKA-guided representational alignment as a practical complement to output matching for quantized LLM recovery.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **背景**：大语言模型（LLM）在延迟和成本受限的生产环境中广泛部署，推动了低精度推理格式（如NVFP4）的采用。NVFP4是一种4比特浮点格式，具有细粒度块缩放和扩展动态范围，相比FP8和INT4能提供显著的吞吐量和内存优势。
- **问题**：后训练量化（PTQ）到NVFP4会导致精度下降。量化感知蒸馏（QAD）通过KL散度让学生模型匹配教师模型的输出分布，能有效恢复精度。然而，仅关注输出匹配可能掩盖内部表示的退化——许多不同的中间激活几何结构都能产生相似的教师对齐logits。
- **核心发现**：通过中心核对齐（CKA）分析发现，仅使用KL散度的QAD会降低逐层表示相似性，尤其是在RL后训练的模型中更为严重。这种内部几何漂移与推理和编码任务的下游瓶颈相关，说明低比特恢复需要保留内部几何结构，而非仅仅匹配输出。

## 2. 论文提出的方法论

### 2.1 核心思想
- 在标准QAD损失（KL散度）的基础上，引入基于CKA的层间表示对齐正则项，同时优化输出分布匹配和内部表示几何结构保持。
- CKA对正交变换和各向同性缩放具有不变性，因此对量化激活的尺度变化鲁棒，适合低比特场景。

### 2.2 关键技术细节
- **基础损失**：采用teacher-top-k变体，只对教师logits中最大的k个（k=8192）词汇计算重归一化后的KL散度（`L_TopK-KL`）。
- **CKA正则项**：选择解码器块的输出作为对齐目标（排除预归一化激活、注意力权重和KV缓存）。线性CKA在特征空间计算，通过协方差矩阵避免显式构造N×N token Gram矩阵。
- **动态损失平衡**：不使用固定λ，而是通过`sg(L_TopK-KL / (L_CKA + ε))`自动缩放CKA项，使两项数值尺度可比。
- 总损失：`L_total = L_TopK-KL + sg(L_TopK-KL / (L_CKA + ε)) * L_CKA`

### 2.3 工程优化
- 仅对教师top-8192词汇计算logit KL，降低通信和内存。
- 使用hook捕获激活，教师激活在`torch.no_grad()`下计算并分离。
- 使用特征空间CKA（`H^T H`矩阵），避免N×N Gram矩阵。
- 动态损失平衡避免手动调参。

## 3. 实验设计

### 3.1 模型与量化设置
- **Nemotron-3-Nano-30B-A3B**：混合Mamba–Transformer MoE模型，经多阶段RL后训练。所有GEMM层量化为NVFP4，除6个自注意力层及其前驱Mamba2块保留BF16。
- **Qwen3-4B-Thinking-2507**：紧凑Transformer推理模型，将注意力和FFN GEMM量化为NVFP4，KV缓存量化为FP8。

### 3.2 基准数据集
- **AIME25**：美国数学邀请赛2025，数学推理（Chain-of-Thought）。
- **GPQA-D**：研究生级Google-Proof Q&A（钻石子集），专家知识。
- **LiveCodeBench-v5**：代码生成与调试（无污染），按难度分Easy/Medium/Hard。

### 3.3 对比方法
- BF16教师（上界）
- NVFP4 PTQ（后训练量化）
- QAD（KL-only）：标准KL蒸馏
- CKA-QAD（本文方法）

## 4. 资源与算力

- 论文明确提到训练配置：Nemotron使用全局batch size 64，学习率5e-6，余弦衰减至5e-7，最大序列长度8192，训练数据约2.5B tokens。
- Qwen3-4B-Thinking-2507的效率分析在H100 GPU上进行，具体GPU数量未明确说明（但从内存占用推断可能为单卡或多卡）。标准QAD每步9.822s，CKA-QAD每步9.871s（+0.5%）；峰值VRAM从73,881 MiB升至79,047 MiB（+7.0%）；全局tokens/秒从53,382降至53,114（-0.5%）；到达验证KL<0.04的步数从4,500降至4,350（-3.3%）。
- 算力描述：相对详细，但未说明完整训练总时长或GPU总数，属于中等程度的透明度。

## 5. 实验数量与充分性

- **主要实验**：在两个不同架构（混合Mamba-Transformer MoE和纯Transformer）上，各使用三个基准（AIME25, GPQA-D, LiveCodeBench），覆盖数学推理、专家知识和代码生成。
- **表示对齐分析**：图1展示了Nemotron上52个匹配解码器块的逐层CKA曲线，对比PTQ、KL-only QAD和CKA-QAD，直观显示内部漂移和恢复。
- **效率分析**：表4详细对比了步时、内存、吞吐和收敛速度。
- **消融**：没有独立的λ调参消融（因采用动态平衡），但通过对比KL-only和CKA-QAD可视为消融CKA正则项的效果。缺乏对层选择策略的消融实验。
- **客观性**：实验设置清晰，对比基线合理，结果数据具体。但仅在两个模型上验证，且均为中小规模（4B/30B A3B），缺乏更大规模模型的验证。
- **充分性评价**：实验相对充分，覆盖了核心假设的验证和下游效果，但缺少更多模型、更多量化格式（如INT4, MXFP4）的消融，以及不同数据混合的敏感性分析。

## 6. 论文的主要结论与发现

- 标准QAD（KL-only）虽然恢复了输出精度，但加剧了内部表示漂移（CKA下降），尤其在RL后训练的模型中（Nemotron最后一层CKA从0.983降至0.740）。
- CKA-QAD通过添加CKA正则化，显著恢复内部表示对齐（Nemotron平均CKA从0.958升至0.994，最后一层从0.740升至0.985），并带来下游任务精度的提升：Qwen3上AIME25提升3.8%，GPQA-D提升1.6%，LiveCodeBench提升1.9%。
- 训练开销很小（步时+0.5%，峰值内存+7.0%）。
- 结论：功能对齐（输出匹配）是必要的但不足以保证能力恢复；保留教师的内部表示几何结构对于需要深度多步特征传播的任务（数学推理、代码合成）尤为重要。

## 7. 优点

- **问题发现新颖**：首次系统揭示了仅输出匹配QAD会导致内部几何退化，用CKA量化了这种退化。
- **方法轻量有效**：CKA正则化简单，对NVFP4激活的尺度变化鲁棒，动态平衡避免调参，训练开销极小。
- **实验设计合理**：在两个不同架构的模型上验证，涵盖数学、知识、代码三类困难任务，并提供了表示对齐与下游性能的关联证据。
- **工程实用性**：提供了详细的工程优化（top-k、特征空间CKA、动态平衡），适合生产部署。
- **理论动机充分**：利用CKA的不变性克服低比特激活的非平稳性，有理论支撑。

## 8. 不足与局限

- **模型规模覆盖有限**：仅验证了4B和30B（激活3B）级别的模型，未在更大模型（如70B、130B）上测试，结论的泛化性存疑。
- **量化格式单一**：只针对NVFP4，未在INT4、MXFP4或其他低比特格式上验证。作者在局限中承认需要进一步探索。
- **层选择策略未充分消融**：仅选择了解码器块输出，未对比其他层（如注意力输出、Mamba状态）或自适应层选择的影响。
- **动态损失平衡的鲁棒性**：`sg(L_TopK-KL / (L_CKA + ε))`可能对损失尺度敏感，论文未提供不同初始化或数据混合下的敏感性分析。
- **缺乏高阶结构约束**：CKA只保留二阶统计量（Gram矩阵），未探索更高阶的流形性质（曲率、局部密度），可能不是最优。
- **计算开销虽小但仍有**：7%的峰值内存增加可能对资源严格受限的场景有影响。
- **实验对比基线不够广泛**：未与其他特征蒸馏方法（如FitNets、Attention Transfer）进行对比，仅与KL-only比较，缺少更全面的蒸馏方法对比。
- **因果归因不够严谨**：虽然CKA恢复与下游精度改善相关，但未提供严格的因果证据（如干预实验）证明CKA对齐是精度提升的直接原因。

（完）
