---
title: Trainable Smooth-Rotation Transforms with Learned Channel Scales for LLM Quantization
title_zh: 可训练的平滑旋转变换与学习通道缩放用于大语言模型量化
authors: "Patrik Czakó, Gábor Kertész, Sándor Szénási"
date: 2026-06-07
pdf: "https://arxiv.org/pdf/2606.09927v1"
tags: ["query:compression"]
score: 9.0
evidence: 后训练量化结合平滑旋转变换用于大语言模型
tldr: "大语言模型量化中激活值异常值通道导致量化误差大。现有基于最大值缩放变换可能过度迁移，本文提出基于高分位数的鲁棒缩放策略替代最大值统计，并引入梯度优化的通道尺度学习。在LLaMA-3.2-1B W4A4量化下，分位数策略降低选定层误差11.1%，联合优化降低12%，训练进一步提升18.5%；全层平均误差降低19.9%。该方法在保持等效变换框架下实现稳健迁移控制和轻量尺度学习，优于固定最大值策略。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.09927v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 855, \"height\": 484, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.09927v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 858, \"height\": 489, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.09927v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 854, \"height\": 427, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.09927v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 851, \"height\": 490, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.09927v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 865, \"height\": 431, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.09927v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 866, \"height\": 423, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.09927v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 887, \"height\": 436, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.09927v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 549, \"height\": 262, \"label\": \"Table\"}]"
motivation: 基于最大值缩放变换可能导致过度迁移，加剧量化误差，需更鲁棒的迁移控制和可学习尺度。
method: 提出分位数鲁棒缩放策略替代最大值统计，结合梯度优化学习通道尺度，在SmoothRot框架内联合优化。
result: "LLaMA-3.2-1B W4A4量化下，分位数策略降误差11.1%，联合优化降12%，训练降18.5%；全层平均误差降19.9%。"
conclusion: 鲁棒迁移控制和轻量尺度学习在等效变换框架下持续改进量化性能，优于固定最大值策略。
---

## 摘要
训练后量化（PTQ）是降低大语言模型（LLM）服务成本最实用的方法之一，但激活值量化仍然困难，因为异常值主导的通道导致较大的量化误差。本文研究了这种退化是否部分由基于缩放的等效变换中的过度迁移引起。我们引入了一种分位数鲁棒的缩放策略，用于SmoothRot风格的变换，通过将基于最大值的激活统计量替换为高分位数，并辅以基于约束梯度的通道缩放优化。在LLaMA-3.2-1B的W4A4量化下，仅分位数策略搜索将选定层误差比SmoothRot基线降低11.1%，联合(alpha, q)搜索降低12%，而训练达到18.5%。将最佳选定层策略应用于所有解码器块的下投影层，将相应的全层平均误差从97.51降低到78.08（降低19.9%）。结果表明，鲁棒的迁移控制和轻量级缩放学习在保持等效变换框架的同时，比基于最大值的固定策略带来了一致的改进。

## Abstract
Post-training quantization (PTQ) is one of the most practical ways to reduce the serving cost of Large Language Models (LLMs), but activation quantization remains difficult because outlier-dominated channels lead to large quantization errors. This paper investigates whether part of this degradation is caused by over-migration in scaling-based equivalent transformations. We introduce a quantile-robust scaling policy for SmoothRot-style transforms by replacing max-based activation statistics with high quantiles, and we complement it with constrained gradient-based optimization of channel scales. On LLaMA-3.2-1B under W4A4 quantization, quantile-only policy search improves selected-layer error by 11.1% over the SmoothRot baseline, joint (alpha, q) search improves it by 12%, and training reaches 18.5%. Replaying the best selected-layer policy on all decoder-block down-projection layers reduces the corresponding full-layer mean error from 97.51 to 78.08 (19.9%). The results show that robust migration control and lightweight scale learning provide consistent gains over max-based fixed policies while preserving the equivalent-transform framework.

---

## 论文详细总结（自动生成）

# 论文详细总结

## 1. 核心问题与整体含义（研究动机和背景）

- **问题**：大语言模型（LLM）的后训练量化（PTQ）中，激活值量化因异常值主导的通道导致较大量化误差，成为瓶颈。
- **现有方法**：SmoothRot 等等效变换通过通道缩放和正交旋转缓解异常值，但基于最大值的缩放（如 SmoothQuant）可能过度迁移量化难度到权重，造成次优性能。
- **本文目标**：探索退化是否源于过度迁移，并提出更鲁棒的缩放策略（分位数替代最大值）及可学习的通道尺度，以在保持等效变换框架下改进低比特量化。

## 2. 方法论：核心思想、关键技术细节

- **核心思想**：在 SmoothRot 变换中，将缩放分量从 max 统计改为高分位数（quantile）统计，减少对稀有异常值的敏感性，并引入梯度下降学习通道尺度，使缩放策略可自适应。
- **变换公式**：对线性层 \(Y = XW\)，构造等效变换 \(A = (H\Lambda)^{-1}\)，其中 \(H\) 为 Hadamard 正交矩阵，\(\Lambda = \text{diag}(s)\)。应用后：\(Y = (XA)(A^{-1}W)\)。
- **缩放因子 \(s_j\) 的改进**：  
  \[
  s_j(\alpha, q) = \frac{Q_q(|X_{:,j}|)^\alpha}{(\max_k |W_{j,k}|)^{1-\alpha}}
  \]
  其中 \(Q_q\) 为第 \(q\) 分位数，\(q<1\) 避免最大值支配；\(\alpha\) 控制激活-权重迁移强度。
- **梯度优化**：采用直通估计器（STE）绕过量化不可微，优化归一化误差 \(\tilde{E}\)，使用 AdamW 优化器，余弦学习率衰减，早停机制。
- **实验流程**：先确定选定层（top-3 critical + 2 normal），然后在选定层上做无训练搜索（N1-N3）和训练搜索（T1-T2），最后全层重放（T3）。

## 3. 实验设计

- **模型与量化配置**：LLaMA-3.2-1B，W4A4（4-bit 权重和激活）。
- **数据集**：WikiText-2，取 256 个样本（每个 128 tokens），80/20 划分训练/测试。
- **基准方法**：
  - **QuaRot**（仅旋转，\(s=1\)）
  - **SmoothRot**（原始 max 缩放，\(\alpha=0.5, q=1.0\)）
- **对比实验组**：
  - **N1**：\(\alpha\) 扫描（21 个值，0.25~0.75，步长 0.025），固定 \(q=1\)。
  - **N2**：\(q\) 扫描（16 个分位数，0.95~1.0 对数间隔），固定 \(\alpha=0.5\)。
  - **N3**：联合网格搜索（N1 和 N2 top-5 候选交叉）。
  - **T1**：从旋转初始化（\(s=0\)）训练，扫描 5 个学习率（5e-3~2e-2）。
  - **T2**：从 N3 最佳策略热启动训练，采用 T1 最佳学习率及其邻值。
  - **T3**：将最佳策略重放至所有 16 个下投影层。

## 4. 资源与算力

- **文中未明确说明**：未提及使用的 GPU 型号、数量、训练时长等具体算力信息。仅提到“在 1B 模型上实现更快实验”，但无量化细节。

## 5. 实验数量与充分性

- **实验数量**：共包含 7 组主要实验（N1 21次、N2 16次、N3 25组合、T1 5次、T2 10次、全层重放 1 次），覆盖无训练和训练两个阶段。
- **充分性**：
  - 系统化分解了 \(\alpha\)、\(q\)、学习率等超参数的影响，且分别探索了独立和联合效应。
  - 在选定层上验证后，通过全层重放验证泛化能力，避免过拟合。
  - 消融对比了 max vs quantile、no-training vs training，逻辑清晰。
- **客观公平性**：所有结果在同一运行集下复现，对比基线为已有公开方法（QuaRot、SmoothRot），实验设计规范。但仅在一个模型和一个数据集上，适用性有限。

## 6. 主要结论与发现

- **无训练场景**：分位数策略（N2）比 alpha-only 调整（N1）更有效，精选分位数（约 0.994）降低选定层误差 11.1%；联合搜索（N3）进一步改善至 12%。
- **训练场景**：从旋转初始化训练（T1）误差降低 18.5%；从 N3 热启动（T2）未明显超越 T1，但两者学习到的相对缩放模式高度相关（Pearson r=0.98），说明优化景观有结构。
- **全层重放**：最佳策略用于所有下投影层，平均误差从 97.51 降至 78.08（降幅 19.93%），且在关键层和正常层均有改善，优于 SmoothRot 和 QuaRot。
- **核心机制**：max 缩放会高估激活尺度导致过度迁移，高分位数可缓解，训练则进一步细化通道间相对模式。

## 7. 优点

- **简洁有效**：仅修改缩放统计量并引入轻量训练，无需改变框架，即可获得一致增益。
- **系统化实验设计**：分阶段探索（no-training→training、选定层→全层），逐步验证每个组件的贡献。
- **提供机理理解**：通过对比 max 与 quantile、分析训练收敛曲线，揭示了过度迁移是退化原因，提出了可操作的缓解方案。
- **代码/可复现性**：方法本身基于公开的 SmoothRot 和 QuaRot，门槛低。

## 8. 不足与局限

- **实验覆盖面窄**：仅使用 LLaMA-3.2-1B 模型和 WikiText-2 数据集，未在更大模型（如 7B、13B）或其他任务（如下游推理、困惑度）上验证，结论的泛化性存疑。
- **优化目标不直接**：以线性输出误差为优化目标，而非最终任务损失（如 perplexity），与下游性能的对齐需进一步确认。
- **训练开销未评估**：虽然训练仅需少量步骤（800 步），但文中未分析实际 GPU 时间、内存增加等，部署 trade-off 未讨论。
- **超参数敏感**：分位数选择（0.994 附近）和训练学习率（0.02 最佳）可能依赖具体配置，迁移至其他模型时需调参。
- **仅针对下投影层**：未扩展至其他线性层（如 Q、K、V 投影），可能错失全局改进潜力。

（完）
