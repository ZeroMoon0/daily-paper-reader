---
title: "ReMP: Low-Downtime Runtime Model-Parallelism Reconfiguration for LLM Serving"
title_zh: ReMP：面向LLM服务的低停机时间运行时模型并行重配置
authors: "Haipeng Yuan, Kaining Zheng, Yongshu Bai, Yuchen Zhang, Yunquan Zhang, Baodong Wu, Xiang Gao, Daning Cheng"
date: 2026-06-17
pdf: "https://arxiv.org/pdf/2606.18741v1"
tags: ["query:eli"]
score: 9.0
evidence: LLM服务的运行时模型并行重配置
tldr: 当前LLM推理系统采用静态的TP和PP并行拓扑，无法适应动态负载，配置切换需重启服务导致分钟级中断和KV缓存丢失。ReMP提出运行时模型并行重配置框架，通过解耦拓扑与运行时状态、设计二维KV缓存迁移机制，实现1-7秒内完成拓扑切换，比重启快数十至上百倍。在动态负载下，ReMP显著优于固定配置，首次实现低延迟在线并行拓扑调整，保证服务连续性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.18741v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 822, \"height\": 453, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.18741v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1634, \"height\": 1046, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.18741v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 850, \"height\": 372, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.18741v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1667, \"height\": 1211, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.18741v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1840, \"height\": 775, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.18741v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 836, \"height\": 300, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.18741v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1823, \"height\": 801, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.18741v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 867, \"height\": 641, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.18741v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 884, \"height\": 471, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.18741v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 878, \"height\": 994, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.18741v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1868, \"height\": 281, \"label\": \"Table\"}]"
motivation: 现有系统静态并行拓扑与动态推理负载矛盾，切换配置需重启导致分钟级服务中断和KV缓存丢失。
method: 解耦模型并行拓扑与运行时状态，设计二维KV缓存迁移机制，实现端到端在线重配置。
result: 7B-70B模型拓扑切换1-7秒，比重启快数十至百倍，动态负载下TTFT、TPOT和吞吐量均优于固定配置。
conclusion: ReMP首次实现低延迟运行时模型并行重配置，提升LLM推理系统的动态适应性和服务效率。
---

## 摘要
当前的大语言模型（LLM）推理系统普遍采用张量并行（TP）与流水线并行（PP）的组合方式来部署超大规模模型。然而，现有系统将模型并行拓扑视为静态配置，无法在运行时灵活调整。这种刚性设计与现实场景中动态变化的推理工作负载之间存在根本矛盾。最先进的系统缺乏在线重配置能力，只能通过重启服务来切换配置，导致数分钟的服务中断、KV缓存丢失以及高昂的重新计算开销。针对此问题，本文提出ReMP，一种支持低停机时间的运行时模型并行重配置框架。ReMP通过三项关键技术实现动态调整：（1）将模型并行拓扑与运行时状态解耦，避免完全服务重建；（2）设计二维KV缓存迁移机制，在TP/PP变更后保留可复用的缓存状态；（3）实现端到端在线重配置。实验表明，在7B至70B参数规模的模型上，ReMP能在1-7秒内完成大部分拓扑切换，相比重启方法实现数十至上百倍的加速。此外，在动态工作负载下，ReMP显著优于固定配置，在TTFT、TPOT和输出吞吐量方面均表现更优。

## Abstract
Current large language model (LLM) inference systems universally deploy ultra-large-scale models using a combination of Tensor Parallelism (TP) and Pipeline Parallelism (PP). However, existing systems treat the model parallelism topology as a static configuration that cannot be flexibly adjusted at runtime. This rigid design creates a fundamental contradiction with the dynamically changing inference workloads in real-world scenarios. State-of-the-art systems lack online reconfiguration capabilities and can only switch configurations by restarting the service, resulting in several minutes of service interruption, KV cache loss, and prohibitive recomputation overhead. To address this problem, this paper presents ReMP, a runtime model parallelism reconfiguration framework that supports low downtime. ReMP achieves dynamic adjustment through three key techniques: (1) decoupling the model parallelism topology from runtime state to avoid full service reconstruction; (2) designing a two-dimensional KV cache migration mechanism to preserve reusable cache states after TP/PP changes; and (3) implementing end-to-end online reconfiguration. Experiments demonstrate that ReMP can complete most topology switches within 1-7 seconds on models ranging from 7B to 70B parameters, achieving speedups of tens to over a hundred times compared to the restart approach. Moreover, ReMP significantly outperforms fixed configurations under dynamic workloads, delivering superior performance in terms of TTFT, TPOT, and output throughput.

---

## 论文详细总结（自动生成）

# 论文总结：ReMP: Low-Downtime Runtime Model-Parallelism Reconfiguration for LLM Serving

## 1. 核心问题与整体含义（研究动机与背景）

- **核心问题**：当前主流LLM推理系统（如vLLM）在部署大模型时，固定采用张量并行（TP）与流水线并行（PP）的静态组合拓扑。然而实际线上工作负载存在显著的动态波动（如每日不同时段的流量差异），静态拓扑无法同时适应低负载（需高TP以降低延迟）和高负载（需高PP以提升吞吐）的需求。现有系统缺乏运行时调整能力，仅能通过重启服务切换配置，导致分钟级服务中断、KV缓存丢失和昂贵的重新计算开销。
- **整体含义**：本文旨在将模型并行拓扑从静态部署参数转变为运行时可动态调整的系统资源，实现低停机时间的在线重配置，从而提升LLM推理服务在动态负载下的适应性和效率。

## 2. 论文提出的方法论

### 核心思想
- 将模型权重、KV缓存、通信组、工作进程和调度器状态从固定的TP/PP拓扑中解耦，使得拓扑切换时只需更新受影响的部分，避免全服务重建。

### 关键技术细节
1. **解耦模型并行拓扑与运行时状态**：
   - **共享模型权重存储**：在服务启动时将完整模型状态（SharedStateDict）载入CPU共享内存；切换时，各工作进程直接从共享内存中选取目标拓扑所需的参数切片，并加载到GPU，避免重新读取检查点文件。
   - **MPU状态空间**：预构建多个候选拓扑的并行状态快照（包含TP组、PP组、排名映射等），切换时直接应用快照，避免动态销毁/重建NCCL通信组。
   - **工作进程生命周期管理**：将工作进程分为激活态和待机态；拓扑切换时按需调整工作集（缩小则部分进程进入待机，扩大则唤醒待机进程），重用进程资源。

2. **二维KV缓存迁移机制**：
   - **逻辑模型**：每个KV缓存块由层ID、块ID和KV头范围唯一标识。PP决定层归属，TP决定头的切片归属。
   - **迁移计划构建**（算法1）：对于每个目标拓扑中的新工作进程，计算其需拥有的层范围（通过PP rank）和头范围（通过TP rank），并与旧拓扑中对应旧工作进程的头范围求交集。每个非空交集生成一个迁移项（源rank、目标rank、层、块集合、头重叠区间）。
   - **局部拷贝与远程传输**：若源和目标rank相同，则仅需本地视图赋值；否则使用异步P2P通信传输。
   - **逐层流式迁移**：按层执行迁移，每层先分配目标存储，完成所有传输后释放旧存储，从而控制峰值内存占用。

3. **端到端在线重配置事务**：
   - 定义了一个事务边界和提交点：调度器在切换期间暂停，当模型分片、KV缓存绑定、MPU状态、调度器缓存配置全部更新完成后，才恢复调度。
   - 利用模型重载与KV迁移可并发执行的特性（两者数据来源和同步依赖不同），将状态变换时间降为 `max(模型重载时间, KV迁移时间)`，而非顺序之和。

## 3. 实验设计

### 使用的数据集/场景
- **工作负载**：基于BurstGPT衍生的请求轨迹，在不同请求速率下回放。
- **场景**：两个8-GPU平台，分别搭载NVIDIA H100和RTX 5090。

### Benchmark与对比方法
- **重配置开销实验**：对比ReMP与传统的重启式切换（重启基线：终止实例后重新启动，加载检查点、重建运行时状态、丢弃现有KV缓存）。
- **服务性能实验**：对比ReMP与两种固定TP/PP基线（TP1PP8：高流水线深度；TP2PP4：较平衡的配置）。ReMP通过快速探测候选拓扑，选择当前负载下加权得分最高（结合输出吞吐、平均TPOT、平均TTFT）的配置。

### 模型
- 模型涵盖7B到70B参数，包括密集架构（Llama2-7B、Llama2-70B、DeepSeek-R1-Distill-Qwen-32B）和MoE架构（Qwen3-30B-A3B）。由于GPU内存限制，Llama2-70B未在RTX 5090上评估。

## 4. 资源与算力

- **硬件平台**：
  - 平台A：8 × NVIDIA H100 GPU + AMD EPYC 7R13 CPU + 2TB 主机内存。
  - 平台B：8 × NVIDIA RTX 5090 GPU + Intel Xeon Gold 6530 CPU + 960GB 主机内存。
- **训练时长**：论文未涉及训练，仅针对推理服务场景。实验中的重配置时间在秒级，未报告整体实验运行总时长。

## 5. 实验数量与充分性

- **重配置实验**：对每个模型（7B、30B、32B、70B）在H100和RTX 5090上测试了多种源/目标拓扑组合（如TP=8/PP=1, TP=4/PP=2, TP=2/PP=4, TP=1/PP=8等），每个组合生成热力图。此外还分析了模型重载与KV迁移的并发效果。
- **服务性能实验**：在H100上对4个模型（Llama2-7B、Qwen3-30B-A3B、DeepSeek-32B、Llama2-70B）在不同请求速率（低到高）下对比了三个方法；在RTX 5090上对2个模型（Llama2-7B、DeepSeek-32B）进行了类似对比。每个场景报告了TTFT、TPOT和吞吐量。
- **充分性评估**：实验覆盖了不同架构（密集和MoE）、不同规模（7B-70B）、不同平台（H100和RTX 5090）、不同拓扑切换方向以及不同负载条件，较为全面。消融实验（重叠优化）也证明了对切换时间的贡献。对比基线（重启、固定拓扑）合理。整体实验设计较充分、客观。

## 6. 论文的主要结论与发现

1. **重配置效率**：ReMP能在1-7秒内完成大多数拓扑切换（7B模型约1-2秒，70B模型约几秒），相比重启方法实现数十到上百倍的加速。
2. **并发优化有效性**：通过重叠模型重载与KV迁移，状态变换时间平均节省13%-32%。
3. **服务性能提升**：在动态请求压力下，ReMP动态选择拓扑的配置相比固定TP1PP8和TP2PP4，显著降低TTFT和TPOT，并提高输出吞吐量。证明最好的TP/PP配置随模型和负载变化，静态配置无法同时适应所有场景。
4. **系统意义**：首次实现了低延迟的运行时模型并行重配置，将拓扑从部署参数转变为可动态调整的资源，为自适应LLM推理服务奠定基础。

## 7. 优点

- **方法创新性强**：提出解耦状态、二维KV缓存迁移、预构建状态快照、工作进程生命周期管理等一套完整的在线重配置方案，解决了传统重启方法的根本性缺陷。
- **工程实现扎实**：基于vLLM V1深度集成，修改执行器、工作管理、KV缓存管理器、通信初始化等核心模块，实现端到端系统。
- **实验覆盖全面**：涵盖多个模型规模、两种主流GPU平台、多种拓扑切换方向、不同负载强度，并包含消融分析。
- **性能提升显著**：切换时间从分钟级降至秒级，加速比高达100x；在动态负载下服务性能（延迟和吞吐）均优于固定配置，证明了实用价值。
- **设计考虑实际**：支持拓扑缩小/扩大、峰值内存控制（逐层流式迁移）、一致性边界等工程细节，适应真实部署需求。

## 8. 不足与局限

- **候选拓扑集合需预知**：MPU状态空间要求预先构建候选拓扑的快照，不支持任意拓扑的即时切换（虽可通过动态组缓存扩展但未实现）。
- **KV缓存容量约束**：当目标拓扑提供的KV缓存容量小于当前活跃缓存占用时，需要抢占请求并回退到重计算，无法完全避免延迟抖动。
- **实验未涉及超大规模集群**：仅在8-GPU系统上验证，未测试更大规模（如跨节点）的拓扑切换性能与可扩展性。
- **服务性能基准仅对比固定配置**：未与已有的动态调度系统（如Llumnix的请求迁移）或分体式架构（如DistServe）比较，但后者不涉及TP/PP拓扑切换，可比性有限。
- **负载模型单一**：仅使用BurstGPT派生轨迹，未涵盖多峰或突发性极强的真实负载模式。
- **未评估重配置期间的服务质量影响**：虽然切换时间短，但调度器暂停期间的请求排队延迟未单独分析。
- **对模型架构的依赖性**：KV迁移机制假设标准Transformer结构，可能不完全适配非自回归模型或特殊注意力变体。

（完）
