---
title: "MosaicQuant: Inlier-Outlier Disaggregation for Unified 4-Bit LLM Quantization"
title_zh: "MosaicQuant: 基于内点-离群点分解的统一4位LLM量化"
authors: "Yangjia Hu, Haodong Wang, Zicong Hong, Qianli Liu, Quanxin Shou, Jian Lin, Song Guo, Xiaowei Shen, Xiangjun Huang, Dian Wang, Jian Yang"
date: 2026-06-14
pdf: "https://arxiv.org/pdf/2606.15652v1"
tags: ["query:compression"]
score: 9.0
evidence: 通过内点-离群点分离的统一4位LLM量化
tldr: 4-bit量化虽能降低LLM内存和加速推理，但难以同时处理密集的常见值和稀疏的离群值，导致精度损失。现有混合精度方法破坏低比特执行统一性，引入额外开销。本文提出MosaicQuant，将权重矩阵拆分为密集4-bit基分量和稀疏4-bit残差分量，前者忠实捕捉内点，后者补偿离群量化误差。同时设计ZipperEngine，将稀疏块计算融合到密集4-bit GEMM内核中，实现统一低比特推理管线。在LLaMA3和Qwen3上，MosaicQuant保持近FP16精度，并相比W16A16基线获得最高1.24倍加速。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.15652v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1392, \"height\": 424}, {\"url\": \"assets/figures/arxiv/2606.15652v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1445, \"height\": 467}, {\"url\": \"assets/figures/arxiv/2606.15652v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1296, \"height\": 712}, {\"url\": \"assets/figures/arxiv/2606.15652v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1154, \"height\": 528}, {\"url\": \"assets/figures/arxiv/2606.15652v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 753, \"height\": 324}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.15652v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1456, \"height\": 559}, {\"url\": \"assets/tables/arxiv/2606.15652v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1297, \"height\": 787}, {\"url\": \"assets/tables/arxiv/2606.15652v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 690, \"height\": 292}, {\"url\": \"assets/tables/arxiv/2606.15652v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1360, \"height\": 1251}, {\"url\": \"assets/tables/arxiv/2606.15652v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1350, \"height\": 1927}, {\"url\": \"assets/tables/arxiv/2606.15652v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1457, \"height\": 1047}, {\"url\": \"assets/tables/arxiv/2606.15652v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1352, \"height\": 497}]"
motivation: 现有4-bit量化难以同时表示密集内点和稀疏离群值，混合精度方法破坏低比特执行统一性，无法实现实际加速。
method: 提出MosaicQuant，将权重矩阵分为密集4-bit基分量和稀疏4-bit残差分量，并设计ZipperEngine将稀疏计算与密集内核融合。
result: 在LLaMA3和Qwen3上保持近FP16精度，相比W16A16基线获得最高1.24倍加速。
conclusion: MosaicQuant实现统一4-bit量化，兼顾精度与速度，为低比特LLM部署提供新范式。
---

## 摘要
4位量化显著降低了大型语言模型（LLM）的内存占用并加速推理。然而，其有限的位宽表示难以同时忠实捕捉密集的常见值（内点）和罕见的大幅值（离群点），导致精度严重下降。现有的混合精度方法通过将离群点保留为高精度来缓解这一问题，但代价是破坏了低位执行的统一性，引入了精度转换和额外的数据移动，削弱了实际加速效果。我们提出MosaicQuant，一种基于内点-离群点分解这一新原理的统一4位LLM量化范式。MosaicQuant并非提升离群点精度，而是将整个权重矩阵量化成一个密集的4位基础分量，其中内点被忠实捕获，而离群点则不可避免地被量化。然后引入一个稀疏的4位残差分量来补偿这些量化误差，选择性地针对输出失真被证明最集中的误差关键权重块。然而，仅统一的表示是不够的，因为若将稀疏残差作为单独内核朴素执行仍会破坏统一的低位推理流水线。为弥补这一差距，我们引入ZipperEngine，它通过重叠流水线将稀疏块计算融合到密集4位GEMM内核中，不仅统一了表示，还将执行统一为单个连贯的低位推理流水线。在LLaMA3和Qwen3上的大量实验表明，MosaicQuant在保持近FP16精度的同时，相比W16A16基线实现了高达1.24倍的加速。

## Abstract
4-bit quantization significantly reduces the memory footprint and accelerates the inference of large language models (LLMs). However, its limited bit-width representation struggles to faithfully capture both dense common values (\emph{inliers}) and rare large-magnitude values (\emph{outliers}), causing substantial accuracy degradation. Existing mixed-precision methods mitigate this by retaining outliers in high precision, but at the cost of breaking the uniformity of low-bit execution, introducing precision conversion and extra data movement that undermine practical speedup. We propose \textbf{MosaicQuant}, a unified 4-bit LLM quantization paradigm built on a novel principle of \emph{inlier--outlier disaggregation}. Rather than elevating outlier precision, MosaicQuant quantizes the full weight matrix into a dense 4-bit base component, where inliers are captured faithfully while outlier are inevitably quantized. A sparse 4-bit residual component is then introduced to compensate for these quantization errors, selectively targeting the most error-critical weight blocks where output distortion is shown to be concentrated. However, a unified representation alone is insufficient, as naïvely executing the sparse residual as a separate kernel still breaks the unified low-bit inference pipeline. To bridge this gap, we introduce \textbf{ZipperEngine}, which fuses sparse block computation into the dense 4-bit GEMM kernel via an overlapped pipeline, unifying not only the representation but also the execution into a single coherent low-bit inference pipeline. Extensive experiments on LLaMA3 and Qwen3 demonstrate that MosaicQuant preserves near-FP16 accuracy while achieving up to $1.24\times$ speedup over the W16A16 baseline.

---

## 论文详细总结（自动生成）

# MosaicQuant 论文详细总结

## 1. 核心问题与整体含义

- **研究动机**：大语言模型参数规模持续增长，4-bit量化能显著降低内存占用并加速推理。然而，4-bit表示的动态范围和分辨率有限，难以同时忠实捕捉密集的常见值（inliers）和罕见的大幅值（outliers），导致精度严重下降。现有混合精度方法将离群点保留为高精度，虽然恢复了部分精度，但引入了精度转换和额外数据移动，破坏了低比特执行的统一性，实际加速效果被抵消。

- **整体含义**：本文旨在设计一种**统一4-bit量化范式**，在不引入高精度回退的前提下，既能处理离群点带来的精度损失，又能保持低比特推理的硬件效率和规则性。

## 2. 方法论

### 核心思想：内点-离群点分解（Inlier–Outlier Disaggregation）

- 将迁移后的权重矩阵分解为两个4-bit分量：
  - **密集4-bit基分量**：对全部权重进行均匀4-bit量化，内点被忠实表示，离群点被粗量化产生较大误差。
  - **稀疏4-bit残差分量**：选择少数最关键的错误块，用4-bit量化残差进行补偿，从而恢复离群点的精度。

### 关键技术细节

1. **块级残差补偿**（Block-wise Residual Compensation）  
   - 观察到量化误差在**块级**（block-level）集中，而非均匀分布在通道上。因此选择块作为基本补偿单元，比通道级更精细高效。

2. **Hessian引导的块选择（∆-Hessian Score）**  
   - 传统直接选择基于输出失真，但计算昂贵（需要重复评估）。本文提出**∆-Hessian分数**，利用对角Hessian估计激活敏感性 \( h_j = \frac{2}{N}\sum_n \hat{X}_{n,j}^2 \)，对每个块B计算：
     \[
     S_B = \left[ \sum_{(i,j)\in B} h_j R_{ij}^2 - \sum_{(i,j)\in B} h_j (R_{ij} - Q(R)_{ij})^2 \right]_+
     \]
   - 该分数估计了应用4-bit残差补偿后的输出误差降低量，且可独立排序，选择Top-K块即可。理论证明在**对角Hessian假设下**是最优的。

3. **统一低比特执行：ZipperEngine**  
   - 关键挑战：若将稀疏残差作为独立内核执行，会引入额外HBM访问和内核启动开销，增加30%以上延迟。
   - ZipperEngine通过**内核融合**与**重叠流水线**解决：
     - 将量化、布局打包、密集4-bit GEMM、稀疏残差计算和reduce-add融合到单个内核中。
     - 按小块（sub-block）重叠执行不同阶段（如Tensor Core执行密集矩阵乘法时，CUDA Core同时准备下一块量化并预取稀疏块），隐藏辅助开销。
   - 最终使稀疏残差计算几乎无额外延迟，保持4-bit推理的吞吐量。

## 3. 实验设计

### 数据集与场景

- **语言建模**：WikiText-2数据集上的困惑度（perplexity）。
- **零样本常识推理**：六个任务（ARC-Challenge, ARC-Easy, HellaSwag, PIQA, LAMBADA, WinoGrande），报告平均准确率。
- **端到端吞吐量**：在Qwen3-8B上，输入长度2048，输出长度128，batch size 4-32，比较吞吐量。

### 模型规模

- LLaMA3.2-3B, LLaMA3-8B
- Qwen3-4B, Qwen3-8B, Qwen3-14B, Qwen3-32B

### 对比方法

- **权重量化**：RTN, GPTQ, AWQ
- **混合精度量化**：ResQ, Atom
- **权重-激活量化**：SmoothQuant, QuaRot, SpinQuant

### 精度设置

- W4A16（权重4-bit，激活16-bit）
- W4A8（权重4-bit，激活8-bit）
- W4A4（权重4-bit，激活4-bit）

### 硬件环境

- **量化阶段**：单张 NVIDIA H20 (96 GB)
- **推理评估**：单张 NVIDIA RTX 4090 (24 GB) + Intel Xeon Gold 6430 + 120 GB DDR5

## 4. 资源与算力

- 论文明确指出在**单张H20 GPU**上执行量化，在**单张RTX 4090**上评估推理性能。未说明训练时长或GPU数量，也未提及校准步骤的具体耗时。因此算力投入描述不完整，仅能推断为单卡运行。

## 5. 实验数量与充分性

- **模型数量**：6个不同规模的模型（3B~32B），覆盖LLaMA和Qwen两大系列。
- **精度设置**：三种主流低比特设置（W4A16, W4A8, W4A4），对比全面。
- **基线方法**：8种，包括最先进的旋转量化（SpinQuant）和混合精度方法（ResQ, Atom）。
- **消融实验**：在Qwen3-4B和LLaMA3-8B上进行，包含：
  - 移除稀疏分支（严重退化）
  - 通道级残差补偿（部分恢复）
  - 完整MosaicQuant（最佳）
- **端到端吞吐量对比**：与TensorRT-LLM、ResQ、AWQ、QuaRot对比，batch size 4-32。
- **内核性能分析**：对比有无ZipperEngine融合的延迟（表5）。
- **开销分析**：内存和延迟开销随模型增加的变化（图5）。
- **公平性**：所有方法使用相同校准数据（128条，2048 tokens），共享解码设置（贪婪解码），在同一GPU上测量。消融实验控制变量。

**总体评估**：实验设计充分，覆盖精度、效率、消融和分析多个维度，对比基线全面且最新，结论可信。

## 6. 主要结论与发现

1. **精度恢复显著**：在W4A8和W4A4下，MosaicQuant均达到近FP16精度，且在所有设置中优于或匹敌混合精度基线。例如在Qwen3-32B W4A8下困惑度8.4（ResQ 9.2, Atom 9.3）。
2. **统一低比特执行有效**：ZipperEngine使得稀疏残差分支的开销极小（内存<10%，延迟<6.2%），实际吞吐量相比W16A16基线提升最高1.24×，相比混合精度方法ResQ提升1.11-1.20×。
3. **块级+激活感知选择优于通道级**：消融实验证明，基于∆-Hessian的块级选择显著优于通道级补偿（准确率提升3-4个百分点）。
4. **稀疏残差预算高效**：仅10%的块预算即可恢复超过一半的输出失真，符合收益递减规律。

## 7. 优点

- **创新性**：提出内点-离群点分解的统一4-bit表示，避免了高精度回退，是首个在表示和执行层面均保持统一低位宽的方法。
- **理论支撑**：证明了∆-Hessian分数在对角Hessian假设下的最优性，提供了理论保障。
- **工程优化**：ZipperEngine通过内核融合和重叠流水线，使稀疏残差几乎无额外开销，实现了可落地的加速。
- **实验全面性**：覆盖多种模型、精度、基线、开销分析，并考察了端到端吞吐量，结果透明可靠。
- **实用性**：仅需少量校准数据（128条），无需额外训练，即插即用。

## 8. 不足与局限

- **模型覆盖有限**：仅在LLaMA3和Qwen3系列上验证，未涉及DeepSeek、Mixtral、Falcon等架构，也未评估MoE模型。通用性有待扩大。
- **激活量化联合优化不足**：本文主要聚焦权重量化，W4A8和W4A4中激活量化采用现有方法（如QuaRot/SpinQuant的旋转），未针对残差补偿设计专门的激活量化策略。可能限制了W4A4下的最优精度。
- **校准数据规模小**：仅使用128条512 tokens的样本，可能无法代表长序列或复杂推理场景。虽然这是常见实践，但过小校准集存在过拟合风险。
- **对角Hessian近似**：忽略跨通道相互作用，在极端离群或强相关场景下可能次优。论文通过理论界分析了误差，但未在更复杂任务（如代码生成、长文档问答）上验证。
- **硬件限制**：所有实验仅在单GPU上进行，未测试多GPU或多节点部署中的通信开销。ZipperEngine的融合策略在多卡场景下是否有效未证明。
- **未与更多系统方法对比**：如TensorRT-LLM的W4A16引擎、vLLM的混合精度支持等，仅与TensorRT-LLM默认配置比较一次。
- **消融仅针对W4A4**：未在W4A8设置下单独消融块选择与通道选择，可能结论不够严谨。

（完）
