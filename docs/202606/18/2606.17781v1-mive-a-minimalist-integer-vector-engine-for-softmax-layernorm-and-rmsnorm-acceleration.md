---
title: "MIVE: A Minimalist Integer Vector Engine for Softmax LayerNorm and RMSNorm Acceleration"
title_zh: MIVE：一种用于Softmax、LayerNorm和RMSNorm加速的极简整数向量引擎
authors: "Kosmas Alexandridis, Giorgos Dimitrakopoulos"
date: 2026-06-16
pdf: "https://arxiv.org/pdf/2606.17781v1"
tags: ["query:eli"]
score: 9.0
evidence: 针对LayerNorm、RMSNorm和Softmax的硬件加速器，降低LLM推理延迟
tldr: 大语言模型推理中，Softmax、LayerNorm与RMSNorm等非线性向量归一化操作成为硬件瓶颈。现有加速器为每种操作定制独立硬件，导致资源重复与硅利用率低下。本文提出MIVE，一个可编程的整型向量引擎，通过挖掘三者公共计算模式，在统一数据路径上实现全部操作。ASIC物理实现表明，MIVE以更小的面积开销取得了优于多数独立加速器的硬件效率，为LLM推理提供高效统一方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.17781v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1880, \"height\": 556, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.17781v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 874, \"height\": 442, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.17781v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 744, \"height\": 379, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.17781v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 924, \"height\": 538, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.17781v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1344, \"height\": 464, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.17781v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 927, \"height\": 224, \"label\": \"Table\"}]"
motivation: 现有加速器为Softmax、LayerNorm和RMSNorm分别设计专用硬件，导致资源重复和硅利用率低下，亟需统一高效方案。
method: 通过分析三种操作的公共计算模式，设计可编程整型向量引擎MIVE，在统一数据路径上执行所有操作，最大化硬件共享。
result: ASIC实现显示，MIVE在面积和硬件效率上优于多数现有独立加速器。
conclusion: MIVE以统一架构高效支持多操作，适合LLM推理加速，减少硬件开销。
---

## 摘要
大型语言模型（LLM）的快速发展加剧了对专用硬件加速器的需求，这些加速器必须满足严格的推理延迟和功耗约束。尽管矩阵乘法占据了总体计算工作量的主导地位，但非线性向量归一化操作（如LayerNorm、RMSNorm和Softmax）可能成为关键硬件瓶颈。现有的加速器通常使用专用的硬件模块来实现这些功能，导致资源重复和硅利用率低下。为了解决这一局限性，我们提出了一种极简整数向量引擎（MIVE），这是一种可编程架构，能够在统一的数据路径内执行所有三种操作。通过利用LayerNorm、RMSNorm和Softmax之间的共同计算模式，所提出的向量引擎最大化了硬件共享，同时减少了实现开销。物理ASIC实现结果表明，MIVE提供了全面的多功能支持，同时在面积和硬件效率方面优于大多数最先进的独立加速器。

## Abstract
The rapid growth of Large Language Models (LLMs) has intensified the need for specialized hardware accelerators that can satisfy stringent inference latency and power constraints. Although matrix multiplications dominate the overall computational workload, non-linear vector normalization operations, such as LayerNorm, RMSNorm and Softmax can become critical hardware bottlenecks. Existing accelerators typically implement these functions using dedicated hardware blocks, leading to duplicated resources and inefficient silicon utilization. To address this limitation, we propose a Minimalist Integer Vector Engine (MIVE), a programmable architecture capable of executing all three operations within a unified datapath. By exploiting common computational patterns across LayerNorm, RMSNorm and Softmax the proposed vector engine maximizes hardware sharing while reducing implementation overhead. Physical ASIC implementation results show that MIVE provides comprehensive multi-function support while achieving higher area and hardware efficiency than most state-of-the-art standalone accelerators.

---

## 论文详细总结（自动生成）

# 论文详细总结

## 1. 核心问题与整体含义（研究动机和背景）
- **问题**：大型语言模型（LLM）推理中，Softmax、LayerNorm 和 RMSNorm 等非线性向量归一化操作成为关键硬件瓶颈。现有加速器通常为每种操作设计独立的专用硬件模块，导致资源重复、硅利用率低下。
- **背景**：LLM 加速主要依赖矩阵乘法（如脉动阵列），但归一化操作依赖昂贵元素变换和向量规约（如指数、平方根、累加、除法），传统方法通过近似（如分段线性近似、位宽缩减）优化单一函数，缺乏统一方案。
- **意义**：提出一个可编程的统一硬件引擎 MIVE，在单一数据路径上执行三种操作，最大化硬件共享，减少面积和功耗开销，提升硅效率。

## 2. 方法论：核心思想、关键技术细节
- **核心思想**：将 LayerNorm、RMSNorm 和 Softmax 分解为公共原语操作集合，包括：
  - 元素级操作（加法、减法、平方、乘加），统一由 `muladd` 单元执行；
  - 向量规约操作（求和、最大值、均值），统一由 `vecsum` 单元执行。
- **关键技术细节**：
  - 迭代计算：将输入向量分成长度为 L 的子向量，逐步处理，并利用累积状态（均值、最大值、平方和）进行校正。
  - 校正算法：
    - **Softmax**：使用指数和缩放更新（公式5），通过 PWL 近似指数和倒数。
    - **LayerNorm**：使用并行方差更新（公式6-7），通过 PWL 近似 `1/√Σ` 和 `(i-1)/i`。
    - **RMSNorm**：只需累加平方和，无需校正。
  - 硬件架构：MIVE 包含：
    - 向量 `muladd` 阵列（并行处理元素），内置 PWL 系数 ROM；
    - 标量 `muladd` 单元（处理校正和归一化因子计算），连接 4 个寄存器（Mold, Mnew, Sold, Snew）；
    - `vecsum` 二元树单元（支持加法和减法，可完成累加和最大值选择）。
  - 数据流：通过 ISA 指令直接控制算术单元和寄存器移动，无需解码逻辑。所有运算使用 INT8 整数算术，固定点表示。
- **公式与算法**（文字说明）：
  - LayerNorm、RMSNorm、Softmax 的完整迭代流程见图1（原文 Fig.1）。
  - 校正算法见 Algorithm 1（LNC）和 Algorithm 2（SMC），均用 `muladd` 和 PWL 实现。

## 3. 实验设计：数据集、Benchmark、对比方法
- **硬件评估**：
  - 使用 28nm 标准单元工艺，Cadence 数字实现工具，目标频率 2GHz。
  - 对比方法：Spagnolo et al. (2022)、Kim et al. (2024)、Softmax Shao et al. (2025)、SOLE (Wang et al., 2023) 等主流独立加速器。
  - 指标：面积 (μm²)、功耗 (mW)、吞吐量 (GOPS)、面积效率 (GOPS/mm²)、功率效率 (GOPS/mW)。
- **LLM 精度评估**：
  - 模型：OPT-30B（使用 Softmax + LayerNorm）、Llama2-7B（使用 RMSNorm）。
  - 基准：LAMBADA（准确率）、Wikitext（困惑度）。
  - 对比：FP16 精确模型 vs. INT8 量化 + MIVE 加速（使用 SMOOTHQUANT 量化方案）。
- **平台**：使用 lm-evaluation-harness 框架进行推理。

## 4. 资源与算力
- **算力**：论文未明确说明使用的 GPU 型号、数量和训练时长。硬件实现基于 ASIC 设计（28nm 工艺），并非 GPU 训练。LLM 精度评估是在 FPGA 模拟或 CPU 上完成？文中未提及具体算力消耗。仅提到在 lm-evaluation-harness 上运行推理测量功率。

## 5. 实验数量与充分性
- 硬件对比实验：1 组，表格 I 列出 5 种方法对比，指标全面（面积、功耗、效率、支持功能）。
- LLM 精度实验：2 组，分别对比 OPT-30B（LAMBADA）和 Llama2-7B（Wikitext）在 FP16 vs INT8+MIVE 下的性能。
- **充分性分析**：实验数量较少，但覆盖了三种归一化操作的代表性模型。未进行多模型、多任务泛化性验证；未与更先进的量化方案（如 LLM.int8()、GPTQ）对比；未进行消融实验（如不同 PWL 分段数、不同位宽的影响）。但考虑到硬件论文通常以面积/效率为主，精度验证作为辅助，基本合理。

## 6. 主要结论与发现
- MIVE 以统一架构支持三种操作，面积效率达 332.6 GOPS/mm²，比 SOLE [8] 高 2.1×，比 Spagnolo [13] 仅软max设计低约 2.1×（但后者仅支持 Softmax）。
- 功率效率 1.7 GOPS/mW，比 SOLE 高 2.8×。
- 在 LLM 推理中，INT8+MIVE 导致的精度退化很小：OPT-30B 准确率从 81% 降至 80%；Llama2-7B 困惑度从 5.8 增至 6.0。
- 证明了通过硬件资源共享可以显著降低面积和功耗，同时保持模型性能。

## 7. 优点
- **创新性**：首次提出统一三种归一化操作的硬件引擎，利用计算模式相似性实现高共享。
- **硬件效率**：在 28nm 下以 4811 μm² 面积实现 2GHz 频率 16 GOPS 吞吐，远超多数独立加速器。
- **完整性**：不仅覆盖 Softmax，还涵盖 LayerNorm 和 RMSNorm，适应现代 LLM 需求。
- **工程落地**：提供物理 ASIC 实现（含布局图），数据可信。

## 8. 不足与局限
- **实验覆盖有限**：仅测试两个模型、两个基准，未在更大规模（如 LLaMA-65B、Falcon）或更多任务（如翻译、对话）上验证鲁棒性。
- **精度评估不充分**：仅使用 SMOOTHQUANT 一种量化方案，未与更优量化方案（如 AWQ、GPTQ）结合；未评估 PWL 近似在不同模型下的误差累积影响。
- **公平性**：对比的硬件工作多采用旧工艺（45nm/28nm），但频率和位宽不同（16bit/12bit vs 8bit），直接比较效率指标时可能存在不公平因素（如精度差异可能导致不同应用场景）。
- **可编程性**：虽然提出 ISA 控制，但未详细说明指令集完整性和灵活性（如是否支持任意向量长度、是否可扩展至其他函数）。
- **资源披露**：未说明硬件设计验证环境、测试覆盖率、功耗测量方法的具体细节，影响可复现性。

（完）
