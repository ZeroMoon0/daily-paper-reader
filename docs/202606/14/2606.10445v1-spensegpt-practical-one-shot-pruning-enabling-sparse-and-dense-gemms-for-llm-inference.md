---
title: "SpenseGPT: Practical One-shot Pruning Enabling Sparse and Dense GEMMs for LLM Inference"
title_zh: SpenseGPT：实用的一次性剪枝方法，实现大型语言模型推理中的稀疏与稠密通用矩阵乘法
authors: "Jaeseong Lee, Seung-won Hwang, Samyam Rajbhandari"
date: 2026-06-09
pdf: "https://arxiv.org/pdf/2606.10445v1"
tags: ["query:compression"]
score: 9.0
evidence: 混合稀疏-稠密剪枝方法，加速LLM推理
tldr: "4稀疏，但严格50%稀疏率会导致后训练剪枝后的精度下降。现有松弛稀疏格式需要编译器支持或产生额外开销。本文提出Spense混合稀疏稠密格式，将权重矩阵分为2:4稀疏区和稠密区，兼容现有高性能库。基于此设计SpenseGPT一步剪枝方法，通过选择重要性高的稠密区域提升效果。在Qwen3-32B和Seed-OSS-36B模型上，B200 GPU使用FP8精度实现1.2倍端到端解码加速，同时保持模型质量。这是首个在B200等GPU上通过半结构化稀疏张量核心实现实际LLM解码加速的一步剪枝工作。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.10445v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 795, \"height\": 460, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.10445v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 730, \"height\": 429, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.10445v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1484, \"height\": 1105, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.10445v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 808, \"height\": 848, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.10445v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 731, \"height\": 433, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.10445v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1634, \"height\": 273, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.10445v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1614, \"height\": 522, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.10445v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 794, \"height\": 321, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.10445v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 778, \"height\": 178, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.10445v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 752, \"height\": 224, \"label\": \"Table\"}]"
motivation: "4稀疏虽提供2倍理论加速，但严格50%稀疏率导致精度损失；现有松弛格式需专用编译器或引入运行时开销，端到端加速受限。"
method: 4稀疏区和稠密区；并设计SpenseGPT一步剪枝方法，通过两种策略选择关键稠密区域。
result: 在Qwen3-32B和Seed-OSS-36B上，B200 GPU FP8精度下获得1.2x端到端解码加速，且保持模型精度。
conclusion: 首次在B200等GPU上实现基于半结构化稀疏的实际LLM端到端解码加速，无需定制编译器，保持模型质量。
---

## 摘要
半结构化2:4稀疏性得到现代加速器的广泛支持，可提供高达2倍的理论加速。然而，其严格的50%稀疏率约束往往在训练后剪枝时导致不可忽略的精度下降。同时，现有的宽松稀疏格式要么需要专门的编译器支持，要么引入限制端到端加速的运行时开销。我们提出Spense，一种实用的混合稀疏-稠密格式，将每个权重矩阵分割成2:4稀疏区域和稠密区域。该设计放宽了有效稀疏率约束，同时保持与现有高性能稀疏和稠密GEMM库的兼容性，避免了自定义编译器支持和输入激活扩展。基于此格式，我们引入SpenseGPT，一种一次性训练后剪枝方法，生成稀疏和稠密区域。值得注意的是，我们表明选择正确的稠密区域至关重要，并设计了两种不同的策略来选择它们。在Qwen3-32B和Seed-OSS-36B上的实验表明，我们的方法在B200 GPU上使用FP8精度实现了高达1.2倍的端到端解码加速，同时保持了精度。据我们所知，这是首个在B200等最新GPU上通过半结构化稀疏张量核心实现真实世界端到端LLM解码加速的一次性剪枝演示，同时保持模型质量。

## Abstract
Semi-structured 2:4 sparsity is widely supported by modern accelerators, providing up to a 2x theoretical speedup. However, its strict 50% sparsity constraint often causes non-negligible accuracy degradation under post-training pruning. Meanwhile, existing relaxed sparsity formats either require specialized compiler support or introduce runtime overheads that limit end-to-end speedup. We propose Spense, a practical hybrid sparse-dense format that splits each weight matrix into a 2:4 sparse region and a dense region. This design relaxes the effective sparsity constraint while remaining compatible with existing high-performance sparse and dense GEMM libraries, avoiding both custom compiler support and input activation expansion. Building on this format, we introduce SpenseGPT, a one-shot post-training pruning method that produces sparse and dense regions. Notably, we show that selecting the right dense regions is important, and we devise two different strategies to choose them. Experiments on Qwen3-32B and Seed-OSS-36B demonstrate that our method achieves up to 1.2x end-to-end decoding speedup on B200 GPUs with FP8 precision, while preserving accuracy. To the best of our knowledge, this is the first one-shot pruning demonstration of real-world end-to-end LLM decoding speedup from semi-structured sparse tensor cores on recent GPUs such as B200s, while maintaining model quality.

---

## 论文详细总结（自动生成）

## 1. 核心问题与整体含义（研究动机和背景）

- **问题**：半结构化 2:4 稀疏性虽可提供最高 2× 的理论加速，但其严格的 50% 稀疏率约束导致训练后剪枝时模型精度显著下降（尤其是在推理、代码生成等复杂任务上）。
- **现有方法的不足**：  
  - 严格 2:4 剪枝（如 SparseGPT）精度损失明显；  
  - 宽松稀疏格式（如 PATCH 需专用编译器，SlideSparse 需输入激活扩展）带来运行时开销或兼容性限制，难以在 B200 等现代 GPU 上实现真正端到端加速。
- **本文目标**：提出一种**实用**的混合稀疏-稠密剪枝格式和对应的一次性剪枝方法，使半结构化稀疏在保持模型质量的同时，能利用现有高性能库（如 cuSPARSELt、cuBLAS）实现真实推理加速。

## 2. 方法论

### 核心思想
- **Spense 格式**：将每个权重矩阵划分为两个连续区域——**稀疏区**（按标准 2:4 模式剪枝）和**稠密区**（保持全精度）。通过输出分割或输入分割实现与现有 GEMM 库的无缝集成，无需定制编译器或激活扩展。
- **SpenseGPT / SpenseGPT+**：面向 GLU 风格 MLP 的一次性剪枝方法。关键在于**选择哪些中间维度索引保留为稠密**，并通过置换使其在运行时连续。

### 关键技术细节
1. **稠密索引选择策略**：
   - **SpenseGPT**：简单将最后 \( p\% \) 的中间索引保留为稠密（利用 SparseGPT 从左到右剪枝时，右侧列可作为补偿缓冲区）。
   - **SpenseGPT+**：基于 Wanda 风格的损失估计器为每个索引计算重要性分数：
     - 对每个线性层（gate、up、down）计算加权幅度 \(\frac{1}{K}\sum_j |W_{ij}|\sqrt{H_{jj}}\)（\(H\) 为 Hessian 对角近似）；
     - 对三个分数归一化后求和，选择得分最高的 \(pN\) 个索引作为稠密区域。
2. **剪枝流程**：
   - 先选稠密索引 → 置换使其位于矩阵右侧/下部 → 在剩余区域上应用 SparseGPT（保持稠密区域不变）。
3. **部署**：输出分割/输入分割两种高效实现，直接调用 cuSPARSELt（稀疏 GEMM）和 cuBLAS（稠密 GEMM）完成计算。

## 3. 实验设计

- **数据集/校准**：使用 128 条来自 s1K-1.1 的样本，序列长度 16384。
- **评测基准**：AIME2024（数学推理）、GPQA Diamond（知识型问答）、IFEval（指令遵循）、LiveCodeBench v6（代码生成）。
- **对比方法**：  
  - **严格 2:4 稀疏**：SparseGPT、Wanda、HyperPrune  
  - **宽松稀疏**：SlideSparse（部署对比）  
  - **本文方法**：SpenseGPT、SpenseGPT+（不同稀疏率 37.5%、25%）
- **测试模型**：Qwen3-32B、Seed-OSS-36B-Instruct
- **部署环境**：B200 GPU，FP8 精度，VLLM 推理框架，批大小 32，序列长度 4096。

## 4. 资源与算力

- 剪枝在 **单块 B200 GPU** 上进行。
- **剪枝耗时**（以小时计）：
  - SpenseGPT：Qwen3‑32B 约 1.33h，Seed‑OSS‑36B 约 1.52h
  - SpenseGPT+：分别约 1.87h、2.15h
  - SparseGPT 耗时与 SpenseGPT 相近；HyperPrune 更慢（约 4h）；ProxSparse 单 GPU OOM。
- 论文未明确说明推理加速测试所用 GPU 数量，但推断为单卡 B200（vLLM 下测量端到端延迟）。

## 5. 实验数量与充分性

- **主要实验**（表 2）：在 2 个模型 × 4 个基准 × 多方法/稀疏率下比较，每组 3 次随机种子取平均，结果稳定。
- **消融实验**（表 3‑5）：
  - 剪枝效率对比（表 3）
  - 稠密索引选择策略对比：SparseGPT 式分数 vs. 本文分数（表 4）
  - 稠密索引布局对比：原地/前端/后端（表 5）
- **加速对比**（图 5）：Spense vs. SlideSparse vs. 稠密基线，显示端到端 1.2× 加速。
- **充分性评价**：  
  - 实验覆盖主流 LLM 系列（Qwen、Seed‑OSS）和多样化任务（推理、知识、指令、代码）；  
  - 对比方法包含最相关的基线，且设置了不同稀疏率；  
  - 消融实验验证了核心设计选择的必要性。  
  - 不足之处：未在更大模型（如 70B+）或更多 GPU 架构（AMD、MTIA）上验证；无训练后微调对比；加速测试仅覆盖一种 batch 规模。

## 6. 主要结论与发现

1. **精度保持**：宽松 2:4 约束（通过保持稠密区域）对维持模型质量至关重要。SpenseGPT+ 在 25% 总稀疏率（12.5% 被保留为稠密）下，在 Seed‑OSS‑36B 上平均得分 73.12，接近密度基线 73.86，远优于严格 2:4 剪枝（65.18）。
2. **端到端加速**：Spense 在 B200 FP8 下实现 1.2× 解码加速，而 SlideSparse 因输入扩展带来的非 GEMM 开销几乎无提升。
3. **稠密索引选择重要性**：索引选择策略对精度影响巨大（如表 4：错误选择得分仅 27.79，正确选择达 70.76）。SpenseGPT+ 的损失估计器优于简单使用 SparseGPT 分数。
4. **布局影响**：将稠密索引置于矩阵右侧/后部是 SparseGPT 下有效补偿的关键（表 5：后端布局 vs 前端/原地得分天壤之别）。

## 7. 优点

- **实用性强**：无需自定义编译器或输入扩展，直接利用现有高速 GEMM 库，降低了部署门槛。
- **精度-效率权衡优秀**：在极小精度损失下获得 1.2× 真实加速，是首个在 B200 上展示端到端加速的一次性剪枝工作。
- **方法简洁有效**：稠密索引选择结合 Wanda 式分数 + 合理布局，在单 GPU 数小时内即可完成剪枝。
- **实验规范**：多次随机种子、公开基线、消融充分，结果可信。

## 8. 不足与局限

- **加速依赖库效率**：实际加速比受稀疏 GEMM 库性能、批大小、序列长度等影响，文中仅展示了特定配置（批大小 32，序列长度 4096）。
- **校准数据敏感性**：SpenseGPT+ 依赖校准分布选择稠密索引，若部署分布与校准分布偏差较大，索引可能不再最优。
- **模型规模与架构覆盖有限**：仅评估 32B/36B 模型和 GLU 结构，未测试更大模型（如 70B+）或其他 Attention 变体。
- **硬件通用性不足**：仅在 B200 (FP8) 上测试，未在 AMD GPU、MTIA 或其他精度下验证。
- **无持续优化**：未与后续微调、蒸馏或量化结合，仅关注一次性剪枝。

（完）
