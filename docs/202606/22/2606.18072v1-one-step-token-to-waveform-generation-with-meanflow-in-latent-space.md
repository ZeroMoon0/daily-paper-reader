---
title: One-Step Token-to-Waveform Generation with MeanFlow in Latent Space
title_zh: 在潜在空间中利用 MeanFlow 实现一步令牌到波形生成
authors: "Zheqi Dai, Guangyan Zhang, Zhen Ye, Jingyu Li, Haolin He, Chunyat Wu, Yiwen Guo, Qiuqiang Kong"
date: 2026-06-16
pdf: "https://arxiv.org/pdf/2606.18072v1"
tags: ["query:mg"]
score: 9.0
evidence: 使用MeanFlow实现一步生成的流匹配方法
tldr: 神经音频编解码器中，Token-to-Waveform解码器因多步流匹配导致推理延迟高。本文提出MeanFlow在高度压缩潜在空间建模平均速度，实现单步生成，无需迭代采样。相比多步基线，实时因子提升17倍，质量损失极小。引入解码器微调与端到端联合微调策略，进一步改善保真度而不增加推理成本。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.18072v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1748, \"height\": 810, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.18072v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1662, \"height\": 274, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.18072v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 884, \"height\": 552, \"label\": \"Table\"}]"
motivation: 解决Token2Wav解码器在多步流匹配中的速度-质量权衡问题，消除高延迟瓶颈。
method: 在压缩潜在空间应用MeanFlow建模平均速度场，实现单步生成；结合解码器微调与端到端联合微调缓解潜在不匹配。
result: 实时因子提升17倍，主观质量近无损；微调策略进一步改善保真度且不增加推理时间。
conclusion: 提出的方法在效率和品质上均优于多步流匹配基线，为低比特率语义编解码器提供了实用的Token2Wav方案。
---

## 摘要
神经音频编解码器是现代基于大语言模型的文本到语音（TTS）及多模态系统的核心。随着低比特率语义编解码器日益受到重视，令牌到波形（Token2Wav）解码器成为决定感知质量与系统效率的关键瓶颈。传统多步流匹配解码器质量优异，但因迭代采样导致推理延迟高，形成严重的质量-速度权衡。本文提出一种新颖的 Token2Wav 架构，通过在高度压缩的潜在空间中应用 MeanFlow 来克服这一限制。通过建模平均速度而非瞬时速度场，MeanFlow 实现了真正的一步生成。在潜在域中操作缓解了波形级流的内存和稳定性问题，相比多步基线方法，实时因子（RTF）提升高达 17 倍，且质量退化可忽略不计。此外，我们引入了缓解潜在不匹配的优化策略，包括冻结 MeanFlow 生成器仅微调解码器，以及端到端联合微调，在不增加推理成本的前提下提升了保真度。代码和演示已公开。

## Abstract
Neural audio codecs are central to modern LLM-based Text-to-Speech (TTS) and multimodal systems. As low-bitrate semantic codecs gain prominence, the Token-to-Waveform (Token2Wav) decoder becomes a bottleneck determining both perceptual quality and system efficiency. Conventional multi-step flow-matching decoders offer superior quality but suffer from high inference latency due to iterative sampling, creating a severe quality-speed trade-off. In this paper, we propose a novel Token2Wav architecture that overcomes this limitation by applying MeanFlow in a highly compressed latent space. By modeling the average velocity rather than the instantaneous velocity field, MeanFlow enables true one-step generation. Operating in the latent domain mitigates the memory and stability issues of waveform-level flows, yielding up to a 17$\times$ improvement in Real-Time Factor (RTF) compared to multi-step baselines with negligible quality degradation. Furthermore, we introduce refinement strategies that mitigate latent mismatch, including decoder-only fine-tuning with the MeanFlow generator frozen and end-to-end joint fine-tuning, improving fidelity without increasing inference-time cost. Code and demo are publicly available.

---

## 论文详细总结（自动生成）

## 论文详细中文总结

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究背景**：现代基于大语言模型（LLM）的文本到语音（TTS）系统普遍采用离散令牌表示：上游模型预测语音令牌，下游 Token2Wav 解码器将令牌转换为波形。神经音频编解码器提供接口，其中语义令牌（低比特率）因紧凑性和可控性被广泛使用，但会丢弃大量声学细节，将恢复韵律、音色等负担转移到 Token2Wav 解码器。
- **核心问题**：现有 Token2Wav 解码器通常基于流匹配（Flow Matching），虽质量高但推理时需数值积分（多步 ODE 采样），导致高延迟和严重速度‑质量权衡。如何在保持高质量的同时实现超低延迟（尤其适用于交互式、端侧应用）是一个关键瓶颈。
- **整体含义**：本文提出在**高度压缩的潜在空间**中应用 **MeanFlow**（建模平均速度场而非瞬时速度场），实现**真正的一步生成**，彻底消除迭代采样开销，同时通过微调策略缓解潜在分布不匹配问题，在接近无损质量下获得数量级的实时因子（RTF）提升。

### 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：将一步生成限定在低维、短序列的潜在空间，避免波形空间生成导致的记忆高消耗和稳定性差问题。
- **整体流程**（见图1）：
  1. **波形 VAE 压缩**：训练轻量级变分自编码器（VAE），将波形编码为低维潜在序列（帧率与语义令牌对齐，如 25 Hz），解码器为确定性映射。
  2. **潜在空间 MeanFlow 生成器**：使用条件 1D Diffusion Transformer（DiT）作为生成器，在潜在空间学习平均速度场，训练时优化 MeanFlow 目标函数，推理时直接从高斯噪声经**一步更新**得到潜在序列：`z_gen = z_1 - f_θ(z_1, 0, 1, c)`。
  3. **微调缓解潜在不匹配**：由于生成潜在分布与 VAE 训练潜在分布可能偏移，提出两种后训练策略（不增加推理成本）：(a) **解码器微调**：冻结 DiT，仅更新 VAE 解码器以适应生成潜在分布；(b) **端到端联合微调**：通过波形域损失回传梯度同时更新生成器和解码器。
- **关键技术细节**：
  - 流路径：使用整流流（Rectified Flow）`z_t = (1-t)z_data + tϵ`。
  - MeanFlow 目标函数：`L_MF = E[w(r,t) ||f_θ(z_t, r, t, c) - sg(¯u_gt)||^2]`，其中 `¯u_gt` 为区间 [r, t] 的平均速度（通过 JVP 计算导数）。
  - 条件注入：通过 adaLN-Zero 将语义令牌嵌入、时间嵌入、说话人嵌入融合到每个 DiT 块。

### 3. 实验设计

- **数据集**：
  - 训练：LibriTTS（全部）。
  - 评估：LibriSpeech test-clean 子集（16kHz，但模型输出 24kHz，评估前重采样至 16kHz）。
- **基准（Benchmark）**：
  - 主基线：CosyVoice2 的 Token2Wav 模块——十步欧拉采样的令牌-梅尔流匹配 + 预训练声码器。
  - 上界：VAE 重建（直接将真实波形编码为潜在再解码）。
  - 语义令牌：使用 CosyVoice2 提取器（25Hz，单码本，词汇量 6561）。
  - 说话人嵌入：CAM++ 预训练模型（192维）。
- **对比方法**：
  - 不同潜在维度（D=8,16,24）。
  - 不同模型容量（140M vs 600M DiT）。
  - 不同微调策略（无微调、仅解码器微调、联合微调）。
- **评估指标**：
  - 词错误率（WER，HuBERT-Large ASR）。
  - 说话人相似度（SpkSim，WavLM-Large）。
  - 无参考语音质量（UTMOS）。
  - 主观平均意见分（MOS，50个随机话语，20名听者，1-5分）。
  - 实时因子（RTF，单块 H20 GPU，FP16，batch size=1，端到端）。

### 4. 资源与算力

- **文中明确说明**：RTF 测量使用 **1× NVIDIA H20 GPU**，FP16 推理，batch size=1。
- **未说明**：训练阶段的 GPU 型号、数量、训练时长等具体算力消耗。因此无法报告训练资源细节。

### 5. 实验数量与充分性

- **实验数量**：
  - 主结果表（表1）：对比基线和最佳配置。
  - 消融实验表（表2）：三个维度——潜在维度（3组）、模型容量（2组）、微调策略（3组）。
  - 主观 MOS 测试：50个话语，20名听者，95%置信区间。
  - 此外进行了 RTF 阶段分解。
- **充分性与公平性**：
  - **优势**：消融设计全面，覆盖关键超参数和设计选择；与强基线 CosyVoice2 在同一条件设置下比较（相同语义 token、说话人嵌入）；MOS 测试有置信区间。
  - **不足**：仅在一个数据集（LibriSpeech test-clean）上评估，缺乏跨数据集泛化验证；仅对比了一个多步基线，未与其它一步生成方法（如 GAN、VAE）比较；主观测试样本量较小（50个），且仅评估英文单人话语；未进行听者间信度分析；600M 模型优于 140M 的结论可能受训练不足影响，实验未给出充分结果。

### 6. 论文的主要结论与发现

1. **速度提升显著**：最佳配置（D=24，140M DiT，联合微调）端到端 RTF 为 0.0046，相比 CosyVoice2（0.0775）**提速 17 倍**。
2. **质量接近无损**：WER 3.41%（基线 3.18%）、SpkSim 0.932（基线 0.940）、UTMOS 3.64（基线 3.76）、MOS 3.85（基线 4.05），退化非常小，尤其考虑到一步生成。
3. **潜在维度影响**：维度从 8 增至 24，质量显著提升；进一步增加可能冗余。
4. **模型容量非越大越好**：600M 模型未优于 140M，推测一步生成对大规模模型易发生过拟合或平均速度估计不稳定，需更深入训练或正则化。
5. **微调策略有效**：Decoder-FT 和 Joint-FT 都能显著改善潜在不匹配，Joint-FT 最优，且不增加推理成本。

### 7. 优点

- **创新性**：首次将 MeanFlow 应用于 Token2Wav 任务，并恰当使用潜在空间压缩，解决了波形空间一步生成的稳定性和效率难题。
- **实用导向**：微调策略在不增加推理负担下提升保真度，且保持一步生成的低延迟，非常适合实时、端侧部署。
- **实验透明**：详细报告了 RTF 分解（生成器 0.0016，解码器 0.0030），消融实验覆盖关键因素，并附有演示与代码链接。

### 8. 不足与局限

- **数据集单一**：仅在 LibriSpeech test-clean 上评估，未在多人、多语种、噪声等场景下验证泛化性。
- **基线局限**：仅与一个多步流匹配基线对比，未与 GAN 解码器、扩散解码、VAE 解码器等其他一步方法系统比较。
- **主观测试规模较小**：50个样本，20名听者，不足以捕捉细粒度差异，置信区间较窄（0.03-0.04）可能受统计方法影响。
- **大规模模型未充分训练**：600M 模型在训练方案下质量不如 140M，可能是训练步数或正则化不足导致，论文未提供训练细节，结论可能不具普遍性。
- **语义 token 固定**：仅使用 CosyVoice2 token，未探索其他语义 tokenizer（如 HuBERT、WavLM），可能存在 tokenizer 偏差。
- **潜在不匹配问题仍需关注**：尽管微调缓解，但生成的潜在分布仍非完美匹配，VAE 重建上界（MOS 4.10）高于最终结果（3.85），表明进一步改进空间。
- **未讨论多说话人 / 情感控制**：当前仅使用说话人嵌入，未涉及更细粒度的声学条件。

（完）
